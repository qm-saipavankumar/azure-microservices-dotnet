name: Wpm

on:
  push:
    branches:
        ["main","master"]


jobs:
  build:
      runs-on: ubuntu-latest
      steps:
       - uses: actions/checkout@v4

       - name: Log in
         uses: azure/login@v1
         with:
           creds: ${{secrets.AZURE_CREDIENTIALS}}
       
      
